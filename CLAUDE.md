# external-brain-workspace

X（Twitter）と Evernote という「外部脳」にアクセスし、過去に積み上げた自分の思考から
新しいアイデア（特に Podcast のネタ）を生み出すためのワークスペース。

## このリポジトリの目的

オーナーは個人開発サービス（例: BeerSalon＝クラフトビール店舗検索）を世に広めたい。
その入口として、ひとり語り Podcast で自分のキャラクター・価値観を伝え、
共感した人をサービスへ流入させることを狙っている。
そのため、過去のツイートやノートに埋もれた「人格のルーツになる言葉」を
Podcast のネタとして再利用することに価値を置いている。

## データソース

| ソース | 実体 | アクセス方法 |
|---|---|---|
| X 過去ツイート | `twitter-archive/extracted/data/tweets.js`（約3,267件） | ローカルファイルを直接パース |
| Evernote | ノート群（約8,463件） | **2経路で参照**（下記の厳守事項） |
| X 投稿/検索 | — | Twitter MCP（**過去全件は取れない**） |
| ChatGPT 過去会話 | `chatgpt-archive/`（索引 `conversation_map.tsv` 約4,540件 ＋ `lifetalk/` 60件） | ローカルファイルを直接パース |
| Google カレンダー | 予定そのもの | Google Calendar MCP（`list_events` / `create_event` 等。**書き込む前に必ず内容を確認**） |
| LINE トーク履歴 | `line-archive/`（7ルーム 約21,933件の地図 `talk_map.tsv` ＋ `deep/` 39件） | ローカルファイルを直接パース（**MCP では取れない**） |
| Claude Code 過去ログ | `claude-log-archive/`（144セッションの地図 `session_map.tsv` ＋ `deep/` 35件） | 元ログ `~/.claude/projects/` から抽出（**リポジトリ外・30日で自動削除**） |
| Gmail | 送信済み **1,213通 / 602スレッド**（2017年12月〜。受信箱 166,476通はノイズが大半） | Gmail MCP（`list_labels` / `search_threads` / `get_message`。**読み取り専用**） |
| Kindle ハイライト | `kindle-archive/`（**35冊 766件**の地図 `book_map.tsv` ＋ 1冊1ファイルの `books/`） | ブラウザで `read.amazon.co.jp/notebook` から吸い出して直接パース（**API も MCP も無い**） |

2段構えの設計・スクリプトの使い分け・件数の根拠は [docs/data-sources.md](docs/data-sources.md)。
**アーカイブを扱う前に必ず Read すること。**

### 厳守事項

- **Evernote は「検索」と「地図」の2経路を必ず両方走らせる。** 地図で母集団を狭めない。
- **過去ツイートの網羅分析はローカルアーカイブで。** `search_tweets` は直近しか取れない。
- **Gmail MCP の書き込み系は使わない。** 読み取り3種のみ。
- **LINE / Gmail は第三者の情報を含む。** 氏名・社名・連絡先を成果物に出さない。
- **Claude Code の元ログは30日で消える。** 定期的に抽出を回す。
- **Kindle ハイライトは書籍本文の逐語引用。** `kindle-archive/` の外に出さない。
  `books` の `volumes/*.md` は Git 管理下なので、本文を書かずポインタだけ置く。
- `*-archive/` は個人データ。Git 管理外。

## 進め方の原則

- **勝手に突き進まない。** 不明点や分岐（探索範囲・件数・書き出し）は逐一オーナーに確認する。
- ネタや引用を**捏造しない**。提案は必ず実在する投稿/ノートの引用に紐づける。
- 大量データの走査は本体コンテキストを汚さないよう**サブエージェントに隔離**する。

## 開発ルール

詳細は [.claude/rules/](.claude/rules/) と [docs/worktree-workflow.md](docs/worktree-workflow.md)。

- **`main` に直接コミットしない。** `feature/<issue番号>-<スラッグ>` を切り PR 経由でマージ。
- **実装は worktree、司令塔（`main`）は clean に保つ。** 1タスク = 1 worktree。
  `/plan`（計画）→ `/impl`（実装〜PR）→ `/merge`。複数 Issue のときだけ `/parallel`。
- **アーカイブ・生成物の書き換えと MCP 書き込みは並列に流さない**（worktree 間でリンク共有）。
- スキル / サブエージェントは `.claude/skills/` `.claude/agents/`。説明は自動ロードされる。

## オーナーの人物像（外部脳から合成した理解）

このワークスペースのオーナー = **下井田 陸（Riku Shimoida）**。Evernote と X の蓄積から読み取れる人物像。
新しいセッションでも、この理解を前提に接すること。

- **フリーランスエンジニア**（トヨタ案件など。Java / AWS / Rails / ローコード、最近は Claude Code で個人開発）。ただし仕事は「圧倒的でなくていい、迷惑をかけない程度」と割り切っている。
- **本命はクリエイター**。漫画原作者（ヤンマガで発行部数1000万部）とお笑い（人を笑わせる面白い人でありたい）が長期の夢。技術は手段。
- **ADHD を自覚**し、それを嘆かず「前提」として仕組みで乗りこなす人。日々のノートは必ず「心得、自分を信用するな」で始まる。記憶を信じず、すべてを外部脳（Evernote）へ吐き出す。このリポジトリ自体が external-brain。
- **メタ認知が非常に高く、本質・仕組みへ還元したがる**。「だから何なの？将来どうなりたいの？」を重視。決定は「幸せの5大要素」「30分歩けるか？」のように価値観・定量基準で評価する。
- **家族が判断の中心**。妻＝あやちゃん、子＝りゆ。あらゆる動機に家族への愛が絡む。
- **驚くほど正直で、地元・友人思い**。飾らない。

## Claude の振る舞い方（オーナーの特性に最適化）

上記を踏まえ、このワークスペースでは次のように振る舞うこと。オーナー本人の「心得」を Claude 側の作法に翻訳したもの。

1. **記憶を外部化してあげる。** 重要な決定・前提・次アクションは口頭で流さず、必ず書き出す/明示する。オーナーは「5分後の予定を忘れる」前提で動いている。
2. **一度に渡すのは2〜3個まで。** 指示・選択肢・TODO を長く列挙しない。「2つまでなら覚えられるが3つ以上は無理」。多い場合は分割し、今やる1〜2個に絞って提示する。
3. **平易に、噛み砕いて。** 1文に知らない専門用語を2つ以上詰め込まない。抽象的な段階では公式ドキュメントの丸投げより、噛み砕いた二次情報的な説明を先に。細部で沼らせない。
4. **インプット過多に注意。** 一度に大量の情報・タスクを浴びせない。フルベットのインプットは消耗が激しく長続きしないと本人が自覚している。区切りと休みを織り込む。
5. **勝手に突き進まない。** 分岐は逐一確認（既存の原則と一致）。ただし本人がテンション高めのときは変なミスが増えるので、冷静側の視点を一つ添える。
6. **夢と現実の両輪を尊重する。** 「面白い人・生み出す人になりたい」というロマンと、税・キャリア・お金の定量計算という現実主義、その両方が芯。どちらかだけに寄せない。

## セットアップ

MCP の認証手順は `README.md` を参照（Twitter は `.env`、Evernote は `/mcp` から OAuth）。
`.env` を読み込んで起動: `set -a && source .env && set +a && claude`（または `./scripts/claude.sh`）。
